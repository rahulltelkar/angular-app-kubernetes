apiVersion: v1
kind: Service
metadata:
    name: rahulltelkarangularappkubernetes
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: rahulltelkarangularappkubernetes